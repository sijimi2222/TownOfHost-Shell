# プロジェクトの作業ルール

## ファイルの変更

- ファイルの読み取り・編集・保存は行ってよい。ただし、その都度のユーザーの指示で指定された範囲を守る。
- 大きな変更をする前に、何を変更する予定か説明する。
- バグ修正では、まず原因を調査してから必要最小限の変更をする。

## ビルドとDLLの配置

- ユーザーが「ビルドして」と指示した場合は、ビルド成功後に、そのビルドで生成された最新の `TownOfHost-Shell.dll` を、現在使用中の Among Us の `BepInEx/plugins` フォルダーへコピーし、既存の同名DLLを置き換える。
- コピー前に、コピー元DLLとコピー先DLLの絶対パスを確認する。現在使用中の Among Us の場所を確認し、プロジェクト設定のパスが現在の使用先と一致するか確認する。使用先を特定できない場合は、推測でコピーせずユーザーに確認する。
- ビルドが失敗した場合はDLLをコピーしない。過去のビルド成果物を代わりにコピーしない。
- コピー前の確認と成功判定を確実に行うため、通常は `-p:CopyToAmongUs=false` を指定してビルドし、成功後に確認済みのパスへDLLをコピーする。
- 配置作業では `TownOfHost-Shell.dll` のみをコピーする。DLL以外のAmong Usファイルは変更しない。
- ユーザーが「ビルドだけ」「コピーしない」などと明示した場合は、その指示を優先する。

## Git操作

- ユーザーの許可なく `git add` / `git commit` / `git push` を行わない。
- ユーザーが「Gitして」と指示したときだけ、その時点の変更内容を確認して `git add` と `git commit` を行う。
- ユーザーが「pushして」または「GitHubに上げて」と指示したときだけ `git push` を行う。「Gitして」だけではpushしない。
- 既存コミットを勝手にamendしない。
